import 'package:flutter/material.dart';

/// Web stub for [image_file_loader.dart].
///
/// The web cannot read local files through `dart:io`, so file-backed images
/// are unavailable and resolve to `null`.
ImageProvider? loadFileImage(String path) => null;
