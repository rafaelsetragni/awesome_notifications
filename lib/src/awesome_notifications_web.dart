/*
_handleWebMethodCall(MethodCall call) {
  switch (call.method) {
    case CHANNEL_METHOD_INITIALIZE:
      return _initializeWeb(call.arguments);

    case CHANNEL_METHOD_CREATE_NOTIFICATION:
      return _createWebNotification(call.arguments);

    default:
      throw PlatformException(
          code: 'Unimplemented',
          details: "The awesome_notifications plugin for web doesn't implement "
              "the method '${call.method}'");
  }
}

_initializeWeb(Map<String, dynamic> arguments) {}

_createWebNotification(Map<String, dynamic> arguments) {}
*/
