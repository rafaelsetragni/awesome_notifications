/// Determines the grouping sort order
/// [GroupSort.Asc] Order the notifications inside grouping by ascending order
/// [GroupSort.Desc] Order the notifications inside grouping by descending order
enum GroupSort {
  /// Order the notifications inside grouping by ascending order
  Asc,

  /// Order the notifications inside grouping by descending order
  Desc
}
